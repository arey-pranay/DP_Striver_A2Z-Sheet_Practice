import java.io.*;
class GFG { 
  static int []term = new int [1000]; 
  static int fib(int n) {
	  // base case
	  if (n <= 1) return n;
	  if (term[n] != 0)	return term[n];
    else{
        term[n] = fib(n - 1) + fib(n - 2);
		    return term[n];
	  }
  }
}
